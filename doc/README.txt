bin contiend l'executable imagimp.exe pour Windows et imagimp.out pour linux
	l'option -f <path_to_file> permet d'ouvrir un fichier ppm avec le programme
	l'option -h vous affiche la liste des options

le Makefile se trouvant à la racine du projet peut compiler programme sur Windows et sur Linux.